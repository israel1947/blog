import { ImagenProfilePipe } from './imagen-profile.pipe';

describe('ImagenProfilePipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenProfilePipe();
    expect(pipe).toBeTruthy();
  });
});
